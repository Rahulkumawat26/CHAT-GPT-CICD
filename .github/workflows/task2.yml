name: Building-testing

on:
    push:
        branches:
            - main
    
jobs:
    build:
        # runs-on: ubuntu-latest
        runs-on: windows-latest
        steps:
            - run: echo "Building..."

    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - run: echo "Testing..."
